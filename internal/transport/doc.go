// Package transport is the WebRTC leg of a Session: one pion PeerConnection
// whose DTLS certificate is bound into the Noise handshake, carrying the
// ordered reliable DataChannel that text and control ride on.
package transport
